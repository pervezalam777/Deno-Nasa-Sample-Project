import { assertEquals } from "https://deno.land/std/testing/asserts.ts";
import {filterHabitatsPlanet} from "./planets.ts";



Deno.test({
    name:"First test function",
    fn: () => {
        assertEquals("world", "world");
    }
})
